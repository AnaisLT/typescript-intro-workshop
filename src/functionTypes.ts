// Function
function sumOfNumbers(a: number, b: number): number {
  return a + b;
}

// Arrow function notation (usually assign to a variable)
const userLoggedIn = (isLoggedIn: boolean): string => {
  return isLoggedIn.length;
};
